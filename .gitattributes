# Normaliza fim de linha: LF no repositorio e na working copy.
* text=auto eol=lf

# Scripts do Windows precisam de CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binarios nunca sao convertidos.
*.jar binary
*.png binary
*.jpg binary
